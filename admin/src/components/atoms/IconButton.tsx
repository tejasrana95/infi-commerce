import MuiIconButton, { IconButtonProps as MuiIconButtonProps } from '@mui/material/IconButton';

export default function IconButton(props: MuiIconButtonProps) {
  return <MuiIconButton {...props} />;
}
